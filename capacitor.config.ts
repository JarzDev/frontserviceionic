import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.agile.servapp',
  appName: 'agileServicios',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
